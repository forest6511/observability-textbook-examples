# ダッシュボードを起動時に自動登録する provisioning 設定。
# 第1章のデモツアーで使う「決済サービスの監視」ダッシュボードを読み込む。
apiVersion: 1

providers:
  - name: default
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
